# agents/registry/.env.example
# Copy to agents/registry/.env and fill in values.
# See root .env.example for full variable reference.

# ── Agent registry ────────────────────────────────────────────────────────────
# Optional — leave blank to disable registry auth
REGISTRY_API_KEY=
